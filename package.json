{
  "name": "urlShortener",
  "version": "1.0.0",
  "description": "URL Shortener Application",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "build": "npm run build:frontend && npm run build:backend",
    "build:frontend": "cd frontend && npm install && npm run build",
    "build:backend": "cd backend && npm install && npx prisma generate && npm run build",
    "postinstall": "npm run build",
    "eb:deploy": "npm run build && eb deploy"
  },
  "dependencies": {
    "express": "^4.18.2",
    "path": "^0.12.7"
  },
  "engines": {
    "node": "20.x"
  },
  "devDependencies": {
    "@types/express": "^5.0.3",
    "typescript": "^5.3.3"
  }
}
